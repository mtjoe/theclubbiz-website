<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="name">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="time">
    <%= f.label :time %><br>
    <%= f.datetime_select(:time, :start_year => Date.today.year, ampm: true) %>
  </div>

  <div class="name">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>  

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
